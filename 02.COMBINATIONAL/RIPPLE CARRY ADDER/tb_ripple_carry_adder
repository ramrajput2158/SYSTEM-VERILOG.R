module tb_ripple_adder;
  logic [7:0]a,b;
  logic cin;
  logic [7:0]sum,cout;
  Ripple_adder dut(.a(a),
                   .b(b),
                   .cin(cin),
                   .sum(sum),
                   .cout(cout));
  initial begin
    $dumpfile("dump.vcd");
    $dumpvars(1,tb_ripple_adder);
    $monitor("Time=%0t 
             a=%b
             b=%b 
             cin=%b
             sum=%b
             cout=%b",$time,a,b,cin,sum,cout);
    for(int i=0;i<2048;i++) begin
      {a,b,cin}=i;
      #10;
    end
    $finish;
  end
endmodule
